import Link from "next/link";

export default function InstallPage() {
  const repository = process.env.NEXT_PUBLIC_SKILLS_REPOSITORY ?? "DeepanshuMishraa/everyday-agent-skills";
  return <div className="install-page"><header><p className="eyebrow">Installation</p><h1>One skill. One complete folder.</h1><p>Every skill installs independently with its instructions and only the supporting references it actually needs.</p></header><section className="install-steps"><article><span>01</span><div><h2>Choose a skill</h2><p>Search by the real-life task, inspect every file in the package, and check its current review status.</p><Link className="text-link" href="/#library">Open the library →</Link></div></article><article><span>02</span><div><h2>Install it globally</h2><p>Copy the command from its skill page. The repository can be configured at build time with <code>NEXT_PUBLIC_SKILLS_REPOSITORY</code>.</p><pre><code>{`npx skills add ${repository}@cook-with-what-you-have -g -y`}</code></pre></div></article><article><span>03</span><div><h2>Or download the folder</h2><p>Download the ZIP and preserve its structure in the skills directory supported by your agent.</p><pre><code>{`skills/\n  cook-with-what-you-have/\n    SKILL.md\n    MEAL-PATTERNS.md\n    FOOD-SAFETY.md\n    agents/openai.yaml`}</code></pre></div></article><article><span>04</span><div><h2>Verify what you installed</h2><p>Compare the package hash with the report shown on the skill page. Changing any included file requires a new review.</p></div></article></section><aside className="install-note"><p className="eyebrow">Compatibility note</p><h2>Agent runtimes differ.</h2><p>The packages use the portable SKILL.md convention with optional supporting files. Check your agent&apos;s documentation for its discovery directory and precedence rules.</p></aside></div>;
}
