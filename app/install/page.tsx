import Link from "next/link";

export default function InstallPage() {
  const repository = process.env.NEXT_PUBLIC_SKILLS_REPOSITORY ?? "DeepanshuMishraa/everyday-agent-skills";
  return <div className="install-page"><header><p className="eyebrow">Installation</p><h1>One folder. One readable file.</h1><p>Each procedure installs independently. There are no scripts, permissions, secrets, or hidden runtime dependencies inside a skill package.</p></header><section className="install-steps"><article><span>01</span><div><h2>Choose a procedure</h2><p>Search by the moment you are handling, then read the objective, workflow, boundaries, and current evaluation status.</p><Link className="text-link" href="/#library">Open the library →</Link></div></article><article><span>02</span><div><h2>Install it globally</h2><p>Copy the command from its skill page. The repository can be configured at build time with <code>NEXT_PUBLIC_SKILLS_REPOSITORY</code>.</p><pre><code>{`npx skills add ${repository}@fridge-to-table-rescue -g -y`}</code></pre></div></article><article><span>03</span><div><h2>Or keep the Markdown</h2><p>Download SKILL.md and place it in the skills directory supported by your agent. The folder name should match the frontmatter name.</p><pre><code>{`skills/\n  fridge-to-table-rescue/\n    SKILL.md`}</code></pre></div></article><article><span>04</span><div><h2>Verify what you installed</h2><p>Compare the file hash with the report shown on the skill page. A changed file must be evaluated again before it can be called tested.</p></div></article></section><aside className="install-note"><p className="eyebrow">Compatibility note</p><h2>Agent runtimes differ.</h2><p>The files use the portable SKILL.md convention: YAML frontmatter followed by Markdown instructions. Check your agent's documentation for its exact discovery directory and precedence rules.</p></aside></div>;
}
